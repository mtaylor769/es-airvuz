{#topVideos}
    <div class="video">
        <a href="/video/{_id}" title="{title}" data-id="{_id}">
            <img data-lazy="{s3Bucket}{thumbnailPath}" alt="{imageAlt}" class="img-responsive" />
        </a>
    </div>
{/topVideos}